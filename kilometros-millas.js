// Kilómetros a millas

// Crear un programa que pida ingresar una cantidad de kilómetros y muestre un mensaje con el resultado de la conversión en millas.

const kms = Number(prompt ("Ingrese una cantidad de kilómetros:"));
const resultado = kms * 0.6213;

alert(`${kms} kilómetros son ${resultado} millas.`);